import {
    LOGIN_REQUEST,
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    CREATE_USER_SUCCESS,
    CREATE_USER_FAIL,
} from "../consts/const";

export function handleLogin(userName) {
    return function (dispatch) {



    }
}
